import voltage
from voltage.flow.login import rpa

def test_main():
    test = voltage.Test()
    browser = test.webdriver_browser()
    #    assert isinstance(browser, voltage.Browser) # Tip for Eclipse
    expected = u"A\u0301" #parse(login.saml.leave_segment_latest_arrival)
    departDate = "A" #browser.get_value(depart_date_locator)
    test.assert_equals(expected, departDate,"Woohoo")
    try:
        browser.execute(test, rpa.Login)
    except:
        test.handle_exception()

    return test.results()
